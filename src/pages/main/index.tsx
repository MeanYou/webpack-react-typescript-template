import React from 'react';

interface MainProps {
    a: number;
}
const Main = (props: MainProps) => {
    return (
        <div>
            <div>Main</div>
        </div>
    );
};

export default Main;